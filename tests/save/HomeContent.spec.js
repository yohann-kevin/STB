import { mount } from "@vue/test-utils";
import HomeContent from "@/components/home/section/HomeContent.vue";
import axios from 'axios';

// describe("Trend.vue", () => {
  // const wrapper = mount(HomeContent,  { mocks: { $axios: axios } });
  
  // it("check title of page", () => {
  //   expect(wrapper.text()).toMatch("Tendance");
  // });

  // it("check partner data is not empty for partner section", () => {
  //   expect(wrapper.findAll('#partner').exists()).toBe(true)
  // });
// });
